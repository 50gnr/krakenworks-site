# Public downloads

The Dungeon Inc. page expects the approved stable APK here as:

`DungeonInc-Defence-0.2.19.apk`

Do not substitute a `*-Tester.apk` build. When promoting a newer stable build,
update the filename and version in `dungeons-inc/index.html` and the note in the
repository README in the same change.
